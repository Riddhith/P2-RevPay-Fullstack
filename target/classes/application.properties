# Application Configuration
spring.application.name=RevPay

# Server Configuration
server.port=8182
server.servlet.context-path=/

# Oracle Database Configuration
spring.datasource.url=jdbc:oracle:thin:@db.freesql.com:1521/23ai_34ui2
spring.datasource.username=RIDDHITH04_SCHEMA_N5CLW
spring.datasource.password=EJTO2$IMHO3bIE5HKTIR8UMIUBO6S8
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# Connection Pool (HikariCP)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# Thymeleaf Configuration
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8

# Session Configuration
server.servlet.session.timeout=30m
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false

# Spring Security
spring.security.user.name=admin
spring.security.user.password=admin

# Multipart (file uploads for loan docs, business verification)
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=20MB

# Logging
logging.config=classpath:log4j2.xml

# JWT Configuration
jwt.secret=RevPaySuperSecretKeyThatIsAtLeast256BitsLongForHS256Algorithm!
jwt.expiration=86400000
jwt.cookie-name=revpay_jwt
